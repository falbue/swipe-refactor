# Code Swipe - Интерактивный рефакторинг кода

Веб-приложение для пошагового улучшения кода через свайп-интерфейс (как Tinder для рефакторинга).

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Конфигурация

Заполните `.env` файл с вашими данными:

```env
# GitHub OAuth
GITHUB_CLIENT_ID=your_client_id
GITHUB_CLIENT_SECRET=your_client_secret

# JWT (сгенерируйте безопасный ключ)
SECRET_KEY=your-secret-key-min-32-chars
```

### 3. Запуск приложения

```bash
python run.py
```

Приложение будет доступно по адресу: `http://localhost:8000`

## 📚 Структура проекта

```
swipe/
├── backend/
│   └── app/
│       ├── api/              # API роуты
│       │   ├── auth.py       # GitHub OAuth
│       │   ├── sessions.py   # Управление сессиями
│       │   └── cards.py      # Работа с карточками
│       ├── core/             # Конфигурация и БД
│       │   ├── config.py     # Настройки
│       │   ├── database.py   # SQLModel + SQLite
│       │   └── __init__.py
│       ├── models/           # Модели данных
│       │   ├── user.py       # Пользователи
│       │   ├── session.py    # Сессии
│       │   ├── card.py       # Карточки кода
│       │   └── __init__.py
│       └── main.py           # FastAPI приложение
├── frontend/
│   ├── templates/
│   │   ├── index.html        # Главная страница
│   │   └── session.html      # Страница сессии
│   └── static/
│       ├── styles.css        # Стили (мобильный дизайн)
│       ├── api-client.js     # API клиент
│       ├── app.js            # Логика главной страницы
│       └── session.js        # Логика сессии + свайпы
├── .env                      # Переменные окружения
├── requirements.txt          # Python зависимости
└── run.py                    # Точка входа приложения
```

## 🎯 Ключевые компоненты

### Backend (FastAPI + SQLMODEL + SQLite)

- **Аутентификация**: GitHub OAuth
- **Модели данных**:
  - `User` - пользователь GitHub
  - `Session` - сессия работы с репозиторием
  - `Card` - карточка кода для свайпа

- **API эндпоинты**:
  - `POST /api/session/` - создание новой сессии
  - `GET /api/session/{id}` - информация о сессии
  - `GET /api/session/{id}/cards/random` - следующая карточка
  - `POST /api/cards/{id}/approve` - одобрить код
  - `POST /api/cards/{id}/edit` - редактировать код
  - `POST /api/session/{id}/create-pr` - создать PR

### Frontend (HTML/CSS/JS)

- **Мобильный дизайн**: адаптивный интерфейс для всех устройств
- **Свайп-интерфейс**: интеграция с Hammer.js
- **Встроенный редактор**: текстовое поле для правок
- **Прогресс-трекинг**: визуализация прогресса сессии
- **Подсветка синтаксиса**: Highlight.js для Python кода

## ⌨️ Горячие клавиши

- **→ (Стрелка вправо)** - одобрить карточку
- **← (Стрелка влево)** - редактировать карточку
- **Space** - пропустить карточку
- **Esc** - закрыть редактор

## 📱 Мобильный дизайн

- Адаптивный grid-layout
- Сенсорные свайп-жесты
- Оптимизированные размеры кнопок (min 44px для мобиля)
- Переносимые элементы на малых экранах
- Полноэкранные модальные окна на мобилях

## 🗄️ База данных

SQLite база данных автоматически создается при первом запуске в файле `swipe.db`.

### Схема БД

```sql
-- Пользователи
users (
  id INTEGER PRIMARY KEY,
  github_id INTEGER UNIQUE,
  github_username TEXT,
  access_token TEXT,
  avatar_url TEXT,
  email TEXT,
  created_at TIMESTAMP
)

-- Сессии
sessions (
  id TEXT PRIMARY KEY,
  user_id INTEGER REFERENCES users(id),
  repo_full_name TEXT,
  base_commit_sha TEXT,
  branch_name TEXT,
  status TEXT,
  total_cards INTEGER,
  created_at TIMESTAMP
)

-- Карточки
cards (
  id TEXT PRIMARY KEY,
  session_id TEXT REFERENCES sessions(id),
  card_hash TEXT UNIQUE,
  file_path TEXT,
  start_line INTEGER,
  end_line INTEGER,
  ast_signature TEXT,
  edited_content TEXT,
  status TEXT,
  created_at TIMESTAMP
)
```

## 🔧 Переменные окружения (.env)

| Переменная | Описание | Пример |
|---|---|---|
| `DATABASE_URL` | URL базы данных | `sqlite:///./swipe.db` |
| `SERVER_HOST` | Хост сервера | `127.0.0.1` |
| `SERVER_PORT` | Порт сервера | `8000` |
| `DEBUG` | Режим отладки | `True` |
| `GITHUB_CLIENT_ID` | ID приложения GitHub | `xxxx...` |
| `GITHUB_CLIENT_SECRET` | Секрет GitHub приложения | `xxxx...` |
| `SECRET_KEY` | Ключ для JWT токенов | `min-32-chars` |

## 📝 TODO (Следующие этапы)

- [ ] Реализовать GitHub OAuth полностью
- [ ] Добавить анализ кода через AST
- [ ] Реализовать локальное клонирование репозитория
- [ ] Добавить коммиты в локальную ветку
- [ ] Реализовать создание PR
- [ ] Добавить поддержку разных языков программирования
- [ ] Реализовать систему рекомендаций
- [ ] Добавить публикацию в Gist

## 🎨 Дизайн

- **Dark theme** для комфортной работы
- **Плавные анимации** для взаимодействия
- **Контрастные цвета** для лучшей читаемости
- **Оптимизированная типография** для кода

## 📄 Лицензия

MIT
